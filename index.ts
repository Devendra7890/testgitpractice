let enum = 1;
console.log(enum);
export {};
// Error: 'enum' is a reserved word and cannot be used as an identifier.
// @filename: index.ts
// Error: 'enum' is a reserved word and cannot be used as an identifier.
let enum = 1;
console.log(enum);